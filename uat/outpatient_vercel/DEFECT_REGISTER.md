# Defect Register — Outpatient Front-End UAT (Vercel)

> **2026-07-08 — Remediation pass complete + deployed + live-verified (per `DEFECT_AND_ISSUE_REMEDIATION_PLAN.md`).** Deployed to Vercel production `avenue-portal.vercel.app` (`dpl_A4BWgjQD…`, commit `cb01636`); D02/D04 exploit curls + money-spine GL re-run live. Summary:
> | ID | Sev | Status (2026-07-08) | Evidence |
> |----|-----|--------------------|----------|
> | **E2E-D02** provider API read scoping | Critical | **FIXED-VERIFIED-LIVE** | Live prod: out-of-scope NWSC member → 404 (was PII+DOB), cross-facility IHK claim → 404, garbage key → 401; in-scope Safaricom/KCB → 200. `tests/api/provider-read-scope.test.ts` (13). |
> | **E2E-D04** preauth create scoping | Critical | **FIXED-VERIFIED-LIVE** | Live prod: out-of-scope preauth (± spoofed providerCode) → 404, **0 rows written** (DB-confirmed); no-auth→401, empty→400. `tests/api/provider-preauth-scope.test.ts` (8). |
> | **E2E-D01** member full-name search | Medium | **FIXED-VERIFIED-LOCAL** | `memberSearchClause` token-AND search; `tests/lib/member-search.test.ts` (8 cases). |
> | **E2E-OBS-MEMSEL** invite selector cap | Low | **FIXED-VERIFIED-LOCAL** | Async `MemberSearchPicker` → `GET /api/admin/members/search` (scoped, unlinked-only); removed 250-cap preload; server-side scope re-check in `inviteUserAction`. |
> | **E2E-OBS-CUR / OBS-2** residual KES labels | Low/Med | **FIXED-VERIFIED-LOCAL** | Global de-KES sweep (271 labels / 64 files); guard now scans 622 files (`npm run currency:guard`). |
> | **OBS-2** mixed-currency settlement / FX | Condition | **FIXED-VERIFIED-LOCAL** | Single-currency batch guardrail (`claim-adjudication.service.ts`); FX-normalised approval (`claim-decision.service.ts` + `FxService`, fail-closed on missing rate). |
> | **OBS-7** fraud approval gate | Condition | **FIXED-VERIFIED-LOCAL** | `ClaimControlService.enforceFraudGate` runs before matrix/side-effects, behind tenant setting. |
> | **OBS-6** clinical nav link | Low | **FIXED** | `Exceptions` nav restricted to `ADMIN_ONLY`; route guard unchanged. |
> Verification: `npm run typecheck` clean · **542/542 vitest pass** · `npm run currency:guard` OK · no lint regressions.


Severity guide (from runbook): **Critical** = cross-scope data exposure, ineligible member claims, rejected line paid, settlement/GL breaks, core workflow crash, duplicate claim/payment. **High** = claim can't be submitted/adjudicated/settled via FE, provider can't access own claims, balances/utilisation update wrong, rejection reason missing. **Medium** = report/export mismatch, unclear validation, missing notification, recoverable awkward flow. **Low** = copy/layout/minor.

| ID | Sev | Persona | Route | Summary | Observed vs Expected | Evidence | Re-test |
|----|-----|---------|-------|---------|----------------------|----------|---------|
| _(none yet)_ | | | | | | | |

## Candidate defects (to verify through the UI before confirming)
| ID | Sev(prov) | Route | Summary | Status |
|----|-----|-------|---------|--------|
| PR-V01 | Medium | /providers | Provider search returns "No providers matching 'Nakasero'"/"'International'" although Nakasero Hospital and International Hospital Kampala (IHK) exist as facility records (present in Invite-User facility dropdown). Search appears not to match existing providers. | OPEN — reproduce & confirm scope |
| **PR-V02** | **Critical** | /settlement | **Provider settlement "Mark Paid" (final settle step) fails deterministically** with a raw Prisma error surfaced to the UI: *"Invalid `prisma.claim.update()` invocation: Transaction API error: A query cannot be executed on an expired transaction. The timeout for this transaction was 5000 ms, however 5271 ms passed…"*. Batch = Aga Khan Jul 2026, 46 claims / KES 3,288,480. Batch is stranded at **CHECKER APPROVED**; SETTLED stays 0; money-out cannot complete end-to-end through the front end. Reproduced twice. Root cause: settle updates each claim in one interactive transaction that exceeds the 5s Prisma limit → does not scale to normal monthly batch sizes. **Also leaks internal DB error text to end user.** | **FIXED-VERIFIED** (Phase R, live build 2026-07-07): set-based settle + timeout; the stranded batch → SETTLED, balanced GL JE, claims PAID, safe error handling. commit 81dcc42. |

## Observations (non-defect)
| ID | Note |
|----|------|
| OBS-1 | After creating a user via the Invite User modal, the Users & Access content pane goes blank until a manual page reload (data present on reload). Low/UX. |
| OBS-2 | Member annual limit shows currency **KES** (e.g. 30,000,000 KES) for the Ugandan NWSC scheme, while provider CPT tariffs are in **UGX**. Currency labelling inconsistency — watch adjudication math for FX handling. |
| OBS-3 | Inline "Update Access" role dropdown on /settings omits PROVIDER USER, so an existing user cannot be converted to provider inline (only via Invite modal, which binds a facility). Likely intentional. |
| OBS-4 | Contract-engine (digital contract) preview panel shows "no contract matched / NO CONTRACT" and payable 0.00 for all lines, contradicting the Adjudicate panel which computed a correct payable ceiling of 16,500 from the same contract PC-2026-128. Misleading preview; adjudicator could wrongly conclude nothing is payable. |
| OBS-5 | Fraud "Contracted Rate Analysis" compares **total claim billed (16,500)** against a **single line's contracted rate (3,500)** → 371.4% variance FRAUD FLAG. Comparison basis looks apples-to-oranges (whole-claim vs one CPT rate); may over-flag legitimate multi-line claims. |
| OBS-6 (RBAC+) | Forbidden route `/settings/exceptions` as CLAIMS OFFICER → branded **"Access Denied"** page at /unauthorized (correct, secure). Minor: the CLINICAL nav still shows an "Exceptions" link that leads to this denial for this role. |
| OBS-7 (control) | A single CLAIMS OFFICER approved a fraud-flagged claim (371% variance, "1 open fraud alert") to the **full billed amount** with **no second-level approval and no fraud-alert clearance** required (approval within payable ceiling ⇒ no override). Prior local build routed APPROVED decisions through a 2-level approval matrix — needs a decision on whether that matrix should apply here. Spine-Q2 relevant; verify in A5/settlement whether a later approval gate catches it. **Closure pass: still persists** — CLOSURE-B carried a FRAUD_FLAG at intake (1 alert) and a single officer partially approved it with no separate fraud-clearance gate. Not a blocker; needs product sign-off. |

## Closure pass (Untested-Items Closure Test Plan, 2026-07-07) — new observations
Full run in `CLOSURE_TEST_RUN_LOG.md`. All six previously-untested items exercised end-to-end, front-end only. **No new Critical/High defects.** Two new observations:

| ID | Sev | Route | Summary | Observed vs Expected | Evidence | Re-test |
|----|-----|-------|---------|----------------------|----------|---------|
| OBS-CLOSURE-1 | Low/Medium | /reports/* → Export PDF | **PDF export could not be verified.** Clicking "Export PDF" opens a new tab at `/api/reports/pdf?reportType=…` that **produces no downloaded file** and renders as a browser "error page" the automation harness cannot read (possibly the native PDF viewer, possibly a server error). CSV export works perfectly and ties out. | Expected: a PDF renders or downloads. Observed: no file; non-readable tab. Needs a manual click to confirm working vs broken. | tab `/api/reports/pdf` error; CSV OK | **FIXED (code, test-verified 2026-07-07).** Root cause: the report page's Export PDF linked to a server **Puppeteer/`@sparticuz/chromium`** route (`/api/reports/pdf`) that is fragile on Vercel serverless AND returned a placeholder for most report types. Replaced with **client-side `@react-pdf/renderer`** (`ExportPDFButton` + rewritten `ReportDocument`) rendering the real KPIs + full table, lazy-loaded on click. New test `tests/components/report-document.test.tsx` asserts valid `%PDF` bytes. **FIXED-VERIFIED LIVE (Vercel dpl_Dn7pdyw…, commit 4b89b5a, 2026-07-07):** Export PDF on the Provider Statements report generated a valid **application/pdf** blob, **66,694 bytes**, magic `%PDF-` (267-record report), no error, no serverless route. |
| OBS-CLOSURE-2 | Medium | /member/notifications | **No in-app member notifications for the outpatient claim lifecycle.** The member Notifications/Alerts inbox stayed empty (UNREAD 0) through claim **intake, approval, and payment** of CLOSURE-A. The member can only see claim status via the dashboard "Recent activity" feed (which correctly showed RECEIVED→APPROVED→PAID). | Expected (per inbox's own copy "approvals, payments, benefits… will appear here"): a decision/payment notification. Observed: inbox always 0. Workaround exists (activity feed). | ss_3592nyeoo (baseline), ss_8678kft10 (post-approval), ss_16532ynaf (post-payment) | **FIXED (code, test-verified 2026-07-07).** Added `MemberNotificationService.notifyForClaim`/`notifyPaidBatch` (fire-and-forget, never break the flow) and hooked them into **intake** (`claim-intake.ts` → CLAIM_STATUS "Visit recorded"), the **canonical decision** (`claim-decision.service.ts` → CLAIM_STATUS approved/partial/declined — covers manual + auto-adjudication), and **settlement** (`markSettlementBatchPaid` → PAYMENT_STATUS "Claim paid"). Dependant claims also notify the **principal** (via `Member.principalId`) so the login-holder sees them. 468/468 tests pass (no regression). **FIXED-VERIFIED LIVE (Vercel dpl_Dn7pdyw…, commit 4b89b5a, 2026-07-07):** filed→approved→settled a fresh claim (CLM-2026-00285, Mark Kato) and the member Alerts inbox went 0→**3** — "Visit recorded" (intake), "Claim approved" (decision, UGX 3,500), "Claim paid" (payment, UGX 3,500), each with correct claim/amount/provider. ss_4377ugfrz. |

### Closure result summary (positives verified live this pass)
- **Partial approval + settlement exclusion (Spine-Q2):** CLOSURE-B billed 14,000 = **approved/paid 6,000 + rejected/excluded 8,000**; settlement batch = 6,000 only; utilisation +6,000 only; report CSVs tie out exactly. Contract-ceiling enforcement blocks over-ceiling approval without a senior override (friendly banner, no raw error).
- **Fund-admin (Branch F1, NWSC self-funded):** fund scoped to NWSC only, balance moved −11,500 = approved payer share (on approval), GL balanced.
- **Scenario D:** zero-amount, future-DOS, double-submit, decide-before-compute, duplicate-settlement all fail safe with friendly validation.
- **HR NWSC-only scope:** sees NWSC utilisation from live claim; denied on all other-employer and admin/provider/claims/settings/member routes.
- **Settlement (PR-V02 fix):** two fresh batches (Aga Khan Aug 11,500; IHK Jul 6,000) settled cleanly, maker≠checker enforced, no duplicate payment, one voucher each, GL balanced.
- **GL posting (revises prior "coverage" concern):** live UI-processed claims post claims-incurred on approval + cash on settlement correctly; the small expense-vs-cash gap is a seed-data artifact, not a live defect.

## E2E Scenario pass (OUTPATIENT_E2E_SCENARIO_UAT_TEST_DOCUMENT, 2026-07-07) — build c11294d
Full log: `E2E_SCENARIO_RUN_LOG.md`. Ran the 13-step spine + a first tranche of Scenario families. **Money spine proven fresh end-to-end** on **CLM-2026-00287** (Mark Kato/Aga Khan, 10,500): eligibility → 3-line intake (RECEIVED) → auto-engine ROUTED (double-capture gate) → claims-officer APPROVED within PC-2026-128 tariff ceiling → maker batch (Oct 2026) → self-approval blocked → distinct-checker approve → **Mark Paid → SETTLED** → **voucher PV-2026-00005 PROCESSED** + **JE-2026-00017** posted → **claim PAID** → **GL ✓ Balanced** → provider statement (TOTAL SETTLED UGX 3,313,980). **No new Critical/High.** Re-verified live: OBS-4 caveat banner, PR-V02 settlement (fresh batch), OP-J03 dup-batch guard, OP-J04 maker≠checker, safe-error handling. Two new low/medium items:

| ID | Sev | Persona | Route | Summary | Observed vs Expected | Evidence | Re-test |
|----|-----|---------|-------|---------|----------------------|----------|---------|
| E2E-D01 | Medium | Admin | /members?q= | Member registry search fails on full-name "First Last" queries: `q=Mark`→28, `q=Kato`→50, but `q=Mark Kato`→**0 of 2999** though "Mark Kato" (NWSC-2026-01768) exists; found only by member number. Multi-token (firstName+lastName) not matched. Same class as fixed provider PR-V01, still open for members. | Expected full-name search returns the member; observed 0. Workaround: member number / single token. | ss_4678y56we, ss_50095prfl, ss_12052dcpa | OPEN |
| E2E-OBS-CUR | Low/Med | All | multiple | Residual **KES** labels on the UGX tenant, inconsistent for the SAME record: CLM-2026-00287 shows UGX on TPA claims list/detail & settlement list but **KES** on provider dashboard/eligibility, **settlement detail/voucher/statement** (PV-2026-00005), GL page, and admin dashboard chart. Stored + GL correct base UGX (balanced) — display only. Broader than the c11294d "authoritative surfaces" fix. | Expected consistent UGX; observed mixed. | ss_0073uzf6h, ss_2826zs4ny, ss_5101gcr23 | OPEN |
| E2E-OBS-MEMSEL | Low | Admin | Invite User → Member User | The member-invite "Member Profile" link selector only loads ~250 members (alphabetical, cuts ~"Angela Kato"); members past that (incl. Mark Kato) can't be linked to a new portal login via UI. Blocks provisioning/repairing member logins for most of the 2,999-member roster. | Expected searchable/full list; observed ~250 cap. | form_input option dump; ss_4673ynhgp | OPEN |

### 🔴 CRITICAL defect found this pass — provider API data-scope (IDOR)
| ID | Sev | Persona | Route | Summary | Observed vs Expected | Evidence | Re-test |
|----|-----|---------|-------|---------|----------------------|----------|---------|
| **E2E-D04** | **CRITICAL** | Provider API (any facility key) | `POST /api/v1/preauth` | **Pre-auth CREATE path not scoped to the API key → cross-tenant/cross-facility write (IDOR).** Deployed `src/app/api/v1/preauth/route.ts`: member by `memberNumber` with no tenant filter (L14), provider by caller-supplied `providerCode` with no key match (L27), PA created under `member.tenantId`+`provider.id` — key identity never used. Any provider key can create pre-auths for arbitrary members in other tenants, attributed to arbitrary facilities. Live route active (empty→400, no-auth→401); same `withApiKey` pattern as live-proven E2E-D02. Live write NOT executed (would pollute other clients' data on prod). | Expected key-scoped member+provider resolution; observed unscoped ⇒ cross-tenant write. | deployed route.ts L14/L27; live 400/401 | **FIXED-VERIFIED-LOCAL (2026-07-08).** Provider resolved from the key (not body); `providerCode` mismatch→403; member confined by `ProviderEntitlementService.entitledMemberWhere` (out-of-scope→404, no PII, no write); same-tenant guard→403; inactive→403 preserved. `tests/api/provider-preauth-scope.test.ts` 8/8. Awaiting live curl re-check on deploy. |
| **E2E-D02** | **CRITICAL** | Provider API (any facility key) | `GET /api/v1/eligibility`, `GET /api/v1/claims?claimNumber=` | **API read endpoints not tenant/provider-scoped → cross-provider + cross-tenant data exposure.** One provider's Bearer key returns **any member's eligibility+PII across all clients** (name, **DOB**, gender, group, package — verified Safaricom Amina Naliaka DOB 1977-03-03 & KCB Agnes Mwangi DOB 1983-04-14 via an NWSC Aga Khan key) and **any claim across all facilities** (member name+number, provider, billed/approved/copay — verified Aga Khan key read **IHK** claim CLM-2026-00284 "Prossy Kato"). Sequential member/claim numbers ⇒ enumerable full-platform PII + claim ledger by any one contracted provider. Auth works (garbage key→401) and claim *creation* is scoped (providerCode spoof forced to own facility); scoping was just never applied to the GET read paths. | Expected provider key resolves only its own facility's claims / contracted members. Observed 200 + full cross-scope PII & financials. | curl elig AVH-DEMO-SAF-0023-S / AVH-2024-00010; claim CLM-2026-00284 (IHK) via Aga Khan key mvxk_0295c8c0…; control garbage-key 401 | **FIXED-VERIFIED-LOCAL (2026-07-08, commits 33b54ae/5b1ae75).** `eligibility`+`benefits` confine member lookup via `ProviderEntitlementService`; `claims` GET via `providerScopeWhere` (own facility only); operator key still spans tenant; garbage key still 401. `tests/api/provider-read-scope.test.ts` 13/13. Awaiting live curl re-check on deploy. |

**Additional Scenario coverage this pass (all PASS, front-end):** A01 eligible / A03 unknown-member; C02 multi-line / C04-06 required-field & zero-line / C08 future-DOS block / C10 facility-spoof; E03 double-capture routing; **F01 full, F02 partial (16,500→8,000), F04 zero-block, F06 over-ceiling block, F07 override path**; J01/J03/J04/J05/J06/J07/J08 settlement/GL/voucher/statement. Re-verified: OBS-4 caveat, **OBS-5 variance 0.0%**, PR-V01 search, PR-V02 settlement.

**Still PENDING (untested = risk, not pass):** live member/HR/fund/reports portal visibility (OP-00.12/13, K01-08) — member blocked by E2E-OBS-MEMSEL, HR/fund/reports need re-provisioning; Scenario B (secure check-in B01-09); D (API/offline/HMS D01-10); H (pre-auth H01-13); I (FX/mixed-currency I02-05 — BLOCKED on KES FX config); remainder of A (A02/A04-08), C (C01/C03/C07/C09), E (E01/E02/E04-10), F (F03/F05/F08-16), J (J02/J09/J10). See `E2E_SCENARIO_RUN_LOG.md` resume pointer.
