import { launch, login, BASE } from './lib.mjs'
const b = await launch()
const p = await b.newPage()
await login(p, 'fund@avenue.co.ke')
await p.goto(BASE + '/fund/cmovmx6dc002x7ouv3mow8517', { waitUntil: 'networkidle2' })
await new Promise(r => setTimeout(r, 2000))
let t = await p.evaluate(() => document.body.innerText.replace(/\s+/g, ' '))
console.log('GROUP PAGE:', t.slice(0, 450))
await p.goto(BASE + '/fund/dashboard', { waitUntil: 'networkidle2' })
await new Promise(r => setTimeout(r, 2000))
t = await p.evaluate(() => document.body.innerText.replace(/\s+/g, ' '))
console.log('\nDASHBOARD:', t.slice(0, 300))
await p.screenshot({ path: 'C:/Coding/avenue-portal/uat/screenshots/17.2-after-deposit.png' })
await b.close()
