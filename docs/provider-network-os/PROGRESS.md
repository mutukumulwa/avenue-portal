# Provider Network Operating System — Progress Board

**Spec:** `PROVIDER_NETWORK_OPERATING_SYSTEM_EXECUTION_PLAN.md` (repo root, 12 phases / 119 packages / 6 gates / 124 scenarios)
**Branch:** `feat/provider-network-os` — created off `feat/claims-autopilot` @ `015cb31` (2026-07-23)
**Why that base:** PNOS consumes the Claims Autopilot rail (§0.1/D5); the rail exists only on `feat/claims-autopilot` (43 commits ahead of `main`, 0 behind, at branch time).
**Isolation rule:** the Claims Autopilot engagement (its F8.2 worker provisioning + human F7.6 campaign + F8.3 k6 staging) continues on ITS branch/prod untouched. Nothing from this engagement is committed to `feat/claims-autopilot` or `main`, and the dirty UAT worktree files (uat/*, scripts/uat-*, the two root plan .md files) are NEVER staged here.

**WORKTREE ISOLATION (2026-07-23):** a concurrent claims-autopilot session shares the main checkout's HEAD. To stop interleaving, `feat/provider-network-os` now lives in a dedicated git worktree at `.claude/worktrees/pnos` (main checkout returned to `feat/claims-autopilot` for the concurrent session). **All PNOS work happens in the worktree.** `node_modules` is symlinked from the main checkout; there is no `.env` in the worktree (intentional — prisma/tests take explicit `DIRECT_URL`/`DATABASE_URL` exports, never the real `aicare_uat`). Throwaway test DB: `postgresql://postgres@127.0.0.1:54329/pnos_uat` (see `TEST_DB_HARNESS.md`).

---

## RESUME PROTOCOL (read this first after any interruption)

1. `cd .claude/worktrees/pnos` (the PNOS worktree; HEAD = `feat/provider-network-os`). If the worktree is gone, recreate: `git worktree add .claude/worktrees/pnos feat/provider-network-os` then `ln -s ../../../node_modules node_modules`. Dirty UAT files live in the MAIN checkout, not here.
2. Read this file's status board; the next package = first `NOT_STARTED` whose dependencies are `COMPLETE` (respect gates below).
3. Read `IMPLEMENTATION_LOG.md` last entry — it names "Next eligible task" and any in-flight partial state.
4. Follow the spec's §0.2 mandatory protocol + §0.3 proof-before-build for that one package. One package per unit of work. Stop at its stop condition, append the §24.5 result note, update this board, commit.
5. Quality bars every package: `npm run typecheck` green; focused tests green; `npm run brand:guard` + `npm run currency:guard` at commit boundaries; schema changes only via sanctioned `prisma db push` workflow (docs/INSTALL.md §3 — never `migrate dev/reset`).

**Statuses:** `NOT_STARTED` · `IN_PROGRESS` · `PARTIAL` · `COMPLETE` · `BLOCKED(reason)` · `GATED(human sign-off named by spec)`

---

## Gates

| Gate | Exit criterion (abbrev.) | Status |
|---|---|---|
| A (F1) | provider/permission/branch scope server-derived on every provider route/API under test | OPEN |
| B (F2) | provider clinical files private, target-authorized, scanned, no permanent public URLs | OPEN |
| C (F4) | structured request completable with independent state/SLA/audit/notification, no financial mutation | OPEN |
| D (F5) | concurrency proves one active sibling; money proves no original mutation/double pay | OPEN |
| E (F6) | provider/admin/export views match; batch/voucher/disbursement/GL conserve | OPEN |
| F (F9) | delivery receipts survive queue/app failure; first real connector passes replay/retry/mapping/reconciliation UAT | OPEN |

## F0 — Baseline and safety characterization

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F0.1 | Freeze current provider route inventory | XS | COMPLETE | `PROVIDER_ROUTE_INVENTORY.md` |
| F0.2 | Characterize provider access leakage (tests) | S | COMPLETE | `tests/api/provider-access-characterization.test.ts` (4 pass) + existing scope suites |
| F0.3 | Characterize claim and PA ownership paths | S | COMPLETE | `CLAIM_PA_OWNERSHIP_PATHS.md` |
| F0.4 | Characterize document storage and consumers | S | COMPLETE | `DOCUMENT_STORAGE_MAP.md` |
| F0.5 | Characterize settlement and money conservation | S | COMPLETE | `SETTLEMENT_MONEY_MAP.md` |
| F0.6 | Create deterministic provider test fixtures | S | COMPLETE | `tests/factories/provider-network.ts` + smoke (3 pass on DB, skip w/o) |

**Phase F0 COMPLETE** (2026-07-23) — all 6 characterization/fixture packages done. Next: **F1 Provider access foundation**, starting F1.1 (seed provider permission catalog). F1 begins actual schema+code changes (additive; flags default OFF). Gate A is the F1 exit.

## F1 — Provider access and entitlement foundation

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F1.1 | Define and seed provider permission catalog | S | COMPLETE | `prisma/seeds/provider-rbac.ts` + `tests/services/provider-rbac-catalog.test.ts` (7 pass) |
| F1.2 | Add provider branch assignments | S | COMPLETE | `ProviderUserBranchAssignment` + `provider-branch-assignment.service.ts` + test (7 pass on DB) |
| F1.3 | Build canonical ProviderAccessService | M | COMPLETE | `provider-access.service.ts` + test (10 pass); dashboard = proof route |
| F1.4 | Migrate provider layout/navigation guards | S | COMPLETE | `provider-nav-model.ts` + `ProviderNav.tsx`/`layout.tsx` migrated + test (9 pass) |
| F1.5 | Harden provider user administration/offboarding | M | COMPLETE | `provider-user-admin.service.ts` + test (6 pass) |
| F1.6 | Extend API keys: scope, expiry, branch, rotation | M | COMPLETE | `ProviderApiKey` fields + service + `provider-api-scopes.ts` + test (7 pass) |
| F1.7 | Enforce API scopes route by route (per group: a,b,c…) | S/grp | PARTIAL | (a) eligibility group DONE — `providerScopeError` + test (4 pass); remaining groups per-unit |
| F1.8 | Audit applicability data readiness | M | COMPLETE | `provider-applicability-readiness.service.ts` + script + test (8 pass) |
| F1.9 | Backfill applicability (reviewed batches) | S/batch | BUILT · GATED(prod --apply needs network-ops signed input) | `provider-applicability-backfill.service.ts` + test (3 pass) |
| F1.10 | Add entitlement shadow comparison | M | COMPLETE | `provider-entitlement-shadow.service.ts` + `ProviderEntitlementShadowSample` + test (4 pass) |
| F1.11 | Make provider browser eligibility canonical | M | BUILT · GATED(enforcement flip needs D3 sign-off; default OFF) | `provider-eligibility.service.ts` + `ProviderEligibilityCheck` + flag + eligibility page rewired + test (5 pass) |
| F1.12 | Enforce entitlement on provider claim submission | M | BUILT · GATED(bypass removed only under D3 flag; default OFF) | `provider-claim-entitlement-gate.service.ts` + claim action wired + test (3 pass) |

**Phase F1 COMPLETE (2026-07-23)** — all 12 packages built. Foundation: permission catalog + persona roles (F1.1), branch assignments (F1.2), ProviderAccessService (F1.3), permission-filtered nav (F1.4), user admin/offboarding (F1.5), scoped/expiring/rotatable API keys (F1.6), per-route scope enforcement — eligibility group (F1.7a), applicability readiness report (F1.8), reviewed backfill mechanism (F1.9), entitlement shadow comparison (F1.10), canonical eligibility (F1.11), claim-submission entitlement gate (F1.12). **Deny-by-default entitlement enforcement (F1.11/F1.12) + backfill apply (F1.9) default OFF/unrun — flipping requires the D3 network-ops/claims/security sign-off (Gate A activation).** Remaining F1.7 route groups (benefits/preauth/claims/upload/hms-batch) are separate per-group units. **Gate A:** foundation server-derived + tested; full per-route permission/branch enforcement activates via the D3 flags at the pilot gate — not flipped in code.

## F2 — Private document foundation

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F2.1 | Private doc metadata + upload-intent schema | M | COMPLETE | `Document` §7.4 fields + `DocumentUploadIntent` + `provider-document.service.ts` + test (6 pass) |
| F2.2 | Resource-level document authorization | M | COMPLETE | `ProviderDocumentService.authorizeTarget` + factory createClaim/createPreauth + test (6 pass) |
| F2.3 | Upload intent creation | S | COMPLETE | `createUploadIntent`/`resolveOpenIntent` + `DOCUMENT_UPLOAD_POLICY` + test (5 pass) |
| F2.4 | Upload finalize + content validation | M | COMPLETE | `finalizeUpload` + `document-mime.ts` (magic-byte) + staging port + test (5 pass) |
| F2.5 | Malware scan + quarantine lifecycle | M | COMPLETE | `provider-document-scan.service.ts` (lease/retry) + `isDocumentUsable` + test (5 pass) |
| F2.6 | Authorized document download | M | COMPLETE | `authorizeDownload` + `document-storage.ts` (MinIO port) + download route (proof) + test (4 pass) |
| F2.7 | Backfill legacy document metadata (per class/batch) | S/batch | COMPLETE (CLAIM class) | `provider-document-backfill.service.ts` + test (4 pass) |
| F2.8 | Migrate document consumers (per group) | S/grp | COMPLETE (provider claim-docs group) | `listTargetDocuments` + `safeScanLabel` + provider claim detail section + test (3 pass) |
| F2.9 | Remove provider public-object access | M | MECHANISM BUILT · BLOCKED(gate NOT ready) | `publicDocumentsEnabled` flag (default=legacy public) + `pnos-document-privacy-readiness.ts` + test (2 pass). Report says NOT ready: 15 direct-`fileUrl` consumers remain + un-backfilled docs. Needs remaining F2.7 batches + F2.8 groups (operator download path) + security sign-off. |

**Phase F2 status:** the private-document engine is COMPLETE and proven (F2.1–F2.8). F2.9's switch is deliberately NOT thrown — flipping it today would break the live admin/member/HR pages that still render public `fileUrl`. **Gate B remains OPEN** pending those consumer migrations + security approval.

## F3 — Canonical PA intake and provider workbench

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F3.1 | Freeze PA submission/decision contracts | S | COMPLETE | `preauth-intake/contract.ts` (v1 normalize/validate/hash) + test (14 pass) |
| F3.2 | PA intake receipt + event schema | S | COMPLETE | `PreauthIntakeReceipt` + `PreAuthorizationEvent` + `preauth-intake/events.ts` + test (7 pass) |
| F3.3 | Implement PreauthIntakeService | M | COMPLETE | `preauth-intake/service.ts` (tx-aware, receipt PROCESSING→ACCEPTED, injectable adjudicate handoff) + test (7 pass) |
| F3.4 | Migrate provider B2B PA submit | S | COMPLETE | `/api/v1/preauth` now adapts over `PreauthIntakeService` (no direct create) + api.preauth.write scope + receipt envelope; route test (8) + E2E-D04 test rewritten (7) |
| F3.5 | Migrate internal PA rails (a/b/c per rail) | S/rail | COMPLETE | **All 3 rails converged on canonical intake+pipeline.** F3.5a member (`d005b3a`, MEMBER_APP, bespoke 15k/CPT auto-approve DELETED, seam test 13). F3.5b admin UI (`c889da7`, ADMIN_PORTAL, fraud now pipeline-enforced, seam test 7). F3.5c tRPC (`8c883c5`, ADMIN_TRPC, input contract unchanged, seam test 5). `ClaimsService.createPreAuth` now called by NO rail → retire in F3.6 |
| F3.6 | Retire fragmented PA persistence | M | COMPLETE | `ClaimsService.createPreAuth` DELETED (`e4e752a`); `preAuthorization.create(` now ONLY in the canonical intake + amendment lifecycle (repo-wide guard test, 3). **CATCH first** (`eeb1d65`): F3.5b/c had dropped the PR-024 benefit-in-package throw and the pipeline's BENEFIT_CAP doesn't backstop it (skips on null config) → added a benefit-in-package gate to the canonical intake (rejects for ALL rails; new `BENEFIT_NOT_IN_PACKAGE` code; 2 mock + 1 real-DB tests). Follow-up: `FraudService.evaluatePreAuth` now dead (fraud.service.ts out of scope) |
| F3.7 | Canonical PA list read model | S | COMPLETE | `preauth-read.service.ts` `PreauthReadService.list(scope)` (`ffd576f`) — tenant + client-confinement (G2.1) + provider-scope + status; consolidates+removes tenant-only `getPreAuthorizations`; tRPC list + admin page rewired WITH confinement (**closed a real gap — PA list wasn't client-confined**). ASSUMPTION: scope inferred from board (mirrors claims read model). FLAG: PA has no branch col (provider-level scope only); tRPC getById confinement deferred to F3.10. Tests 8 |
| F3.8 | Provider PA list page | S | COMPLETE | `app/provider/preauth/page.tsx` (`7406e54`) — read-only provider PA list via F3.7 read model (providerId-scoped), server-authorized by `providerPermits(perms,"provider.preauth.read")` (new pure guard, legacy-compatible). Nav gains Pre-auth item (Care group, `preauth`→ShieldCheck). Tests +4. **NOT browser-verified** (no .env in worktree; :3000 held by main-checkout server; no seeded provider session) — read-only mirror of proven claims page, logic unit-covered. Read-only: create=F3.9, detail=F3.10 |
| F3.9 | Provider PA submission page | M | COMPLETE | `app/provider/preauth/new/{page,form,actions}` (`c0f5e04`) — submits via `PreauthIntakeService` on the **PROVIDER_PORTAL** (provider-bound) channel: facility from session (D1), member entitlement-gated inside intake (D3 flag OFF=tenant-only), `executeAutoDecision` handoff; gated by `provider.preauth.create`; ACTIVE-contract required. List page gains permission-gated "New" button + submitted banner. Action unit-tested (7). **NOT browser-verified** (env). ASSUMPTION: mirrors claims/new |
| F3.10 | Canonical PA detail read model/page | M | COMPLETE | `PreauthReadService.getById(scope,id)` (`1b1fba7`) — non-enumerating (out-of-scope→null), tenant+client+provider scope; retired `ClaimsService.getPreAuthById` (all 3 callers migrated). **Closed tRPC getById confinement gap** (was tenant-only)→NOT_FOUND; admin detail confined. New `app/provider/preauth/[id]/page.tsx` read-only detail (provider-scoped 404, event timeline via F3.2, provider-safe fields); list rows link to it. Tests +8. FLAG: provider-detail documents section deferred (F2.8 analogue). NOT browser-verified |
| F3.11 | Provider PA cancellation | M | COMPLETE | `provider/preauth/[id]/actions.ts` `cancelProviderPreauthAction` (`4997b30`) — `provider.preauth.cancel` gate + ownership via F3.10 scoped read (non-enum not-found) + pre-use state gate (SUBMITTED/UNDER_REVIEW/APPROVED) → canonical `preauthAdjudicationService.cancelPreAuth` (releases hold PR-011#3, CANCELLED, audit). `CancelPreauthButton` on detail page (reason+confirm). Action tested (6). NOT browser-verified |
| F3.12 | Provider PA amendment | M | COMPLETE | `amendProviderPreauthAction` (`55226e4`) — gate `provider.preauth.create` (no dedicated amend perm; ASSUMPTION flagged) + ownership/APPROVED-parent via F3.10 scoped read → canonical `preauthAdjudicationService.createPaAmendment` (linked PA-AMD; was UNWIRED, F3.12 first caller) → same `executeAutoDecision` pipeline. `AmendPreauthForm` on detail page (APPROVED only). Action tested (7). NOT browser-verified |
| F3.13 | PA-to-claim prefill and submit | M | COMPLETE | `fileClaimFromPreauthAction` (`735bc6c`) — gate `provider.claim.create` + ownership via F3.10 scoped read → canonical `ClaimsService.createClaimWithPreauth` (prefills from PA + submits via ClaimIntakeService `preauthConversion`, idempotent, APPROVED-only) + `PREAUTH_ATTACHED` audit → redirect to claim. `FileClaimButton` on detail (APPROVED). Action tested (4); audit-coverage green. NOT browser-verified |
| F3.14 | Authorized GOP/LOU artifact | M | COMPLETE | `gop-artifact.ts` `buildGopData` + `GopDocument`/`GopButton` (`55f860b`) — downloadable Guarantee of Payment (@react-pdf, DebitNote pattern) for an APPROVED PA on the provider-scoped detail page; pure mapper returns null unless APPROVED+gopNumber. Authorized by the page's existing gate (not a stored file → no F2 flow). Mapper tested (4). NOT browser-verified. LOU = existing admin/cross-border artifact |

**Phase F3 COMPLETE (2026-07-24, tip after `55f860b`)** — the canonical PA rail end-to-end. **Write:** contract (F3.1) + receipt/event schema (F3.2) + PreauthIntakeService (F3.3); every rail converged on it — B2B API (F3.4), member (F3.5a), admin UI (F3.5b), tRPC (F3.5c); fragmented persistence retired to a single creator (F3.6) + a benefit-in-package guard added canonically (F3.6 CATCH, restores PR-024 for all rails). **Read:** canonical scoped list (F3.7, client-confined) + detail (F3.10, non-enumerating); retired `getPreAuthorizations`/`getPreAuthById`. **Provider surface:** list (F3.8), submission (F3.9), detail (F3.10), cancellation (F3.11), amendment (F3.12), PA→claim (F3.13), GOP artifact (F3.14) — all server-authorized via `providerPermits` + provider-scoped reads. **CAVEATS:** F3.7+ built from the board's one-line descriptions (detailed plan not in-repo) with assumptions flagged per package (see IMPLEMENTATION_LOG); all provider UI is service/action-unit-tested but **NOT browser-verified** (worktree has no .env; :3000 is a foreign server) — visual check deferred to a run with env+seed or post-merge. Suite 1276 pass/191 skip; tsc+brand+currency green. **Next: F4** (information requests, inbox, SLAs, notifications) — 10 packages.

## F4 — Information requests, inbox, SLAs, notifications

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F4.1 | Information-request schema + catalogs | M | COMPLETE | `PreauthInfoRequest` model + `PreauthInfoRequestStatus` enum (`105a7a6`) — relation-less PA satellite (F3.2 pattern), UNIQUE(paId,sequence), inbox+SLA indexes, mutable OPEN→RESPONDED→ACCEPTED/REOPENED/CLOSED/CANCELLED. Pure catalog `preauth-info-request/catalog.ts` (8 item types + normalize/validate/label). Additive schema (pushed to throwaway PG; prod via build). Tests: catalog 5 + **real-DB schema 4**. ASSUMPTION: model shape inferred from the F4 lifecycle rows |
| F4.2 | Request open/cancel service | M | COMPLETE | `PreauthInfoRequestService.open/cancel` (`5787af9`) — open: catalog-normalized items + prompt + PA pre-decision gate → tx {seq + OPEN row (provider/member/client scope, 72h SLA) + INFO_REQUESTED event, safe metadata}; cancel: live→CANCELLED + INFO_REQUEST_CANCELLED event, terminal-state guards; typed `InfoRequestError`. No PA status change. Added `INFO_REQUEST_CANCELLED` event type; factory teardown clears the satellite. **Real-DB test (4)**. Actor-parameterized (perms gated by F4.4 surface) |
| F4.3 | Provider draft + explicit response submit | M | COMPLETE | `submitResponse` service + `submitInfoResponseAction` (`f366374`) — RESPONDABLE gate (OPEN/REOPENED), facility ownership via providerId scope (non-enum NOT_FOUND), tx {RESPONDED + responseNote + RESPONSE_SUBMITTED event}; action gated `provider.preauth.respond` + compliance `PREAUTH_INFO_RESPONSE_SUBMITTED` audit. **ASSUMPTION: "draft"=client-side form (F4.7); server persists only the explicit submit** (no resumable-draft field). Real-DB service (5) + action (4). Page=F4.7 |
| F4.4 | Reviewer accept/reopen/close | M | COMPLETE | `accept/reopen/close` service (shared `applyDecision`) + admin actions (`a252014`) — accept: RESPONDED→ACCEPTED+RESPONSE_ACCEPTED (sanctions F4.5); reopen: →REOPENED+RESPONSE_REOPENED (respondable again); close: live→CLOSED+INFO_REQUEST_CLOSED; typed guards. Admin actions `requireRole(CLINICAL)` + `PREAUTH_INFO_ACCEPTED/REOPENED/CLOSED` audit. Added 2 event types. Real-DB service (6) + actions (5). ASSUMPTION: reviewer=CLINICAL role (no dedicated perm). UI=F4.7 |
| F4.5 | Sanctioned claim reprocessing after acceptance | S | COMPLETE | `PreauthInfoRequestService.listReprocessable(scope)` (`f208141`) — **DECISION (user): mark-sanctioned/human-re-decides, NO auto pipeline re-run** (money spine untouched). Surfaces PAs with an ACCEPTED info request + still-undecided PA (client-confined); RESPONSE_ACCEPTED is the sanction marker; human re-adjudicates via the existing workbench. Pure read (no decide/hold). Real-DB test (7). FLAG: queue UI = F4.6/F4.7; "claim"→PA re-decision (info reqs are PA-scoped) |
| F4.6 | Canonical provider inbox projection | M | COMPLETE | `preauth-info-request/inbox.ts` `providerInboxProjection(scope)` (`d962299`) — provider's awaiting-it info requests (OPEN/REOPENED default; widenable) + PA/member context (relation-less 2-step), dueAt-asc SLA order + `overdue` flag, provider-scoped. Pure read. Real-DB test (1). FLAG: info-requests only (unionable later); UI=F4.7 |
| F4.7 | Inbox list + info-request detail pages | M | COMPLETE | Provider `/provider/inbox` (list via F4.6 projection) + `/provider/inbox/[id]` (detail + respond via F4.3) + `RespondForm` (`6193427`); new `getForProvider` (non-enum) + nav "Inbox" item (Home, `provider.preauth.read`)→Inbox icon. Nav test (+1) + real-DB service (+1). **FLAG: reviewer admin UI (open/accept/reopen/close triggers on admin PA detail) NOT built — F4.2/F4.4 actions exist but need UI wiring (follow-up).** Pages NOT browser-verified |
| F4.8 | Notification/outbox schema + dispatcher | M | COMPLETE | `NotificationOutbox` model + `NotificationOutboxService` (`f76bf95`) — **transactional outbox** (chosen: email worker unprovisioned): enqueue (idempotent dedupeKey) → dispatch via **pluggable delivery port** (IN_APP→SENT now; EMAIL→port, default SKIPPED "not provisioned"→future worker plugs in) + `listProviderNotifications`/`markRead` (provider-scoped). Additive schema (throwaway PG; prod via build). Real-DB test (4). No producer/sweeper yet (F4.9/F4.10) |
| F4.9 | Migrate provider events to dispatcher (per family) | XS/fam | COMPLETE | Info-request family → outbox (`18d9d6d`): open/reopen/cancel/accept/close each `NotificationOutboxService.enqueue` an IN_APP provider notification **in the same tx** (transactional, exactly-once) w/ href + safe metadata. Reviewer family (submitResponse) deferred. Real-DB test (+1, JSON-metadata match). Rows PENDING until F4.10 sweeper |
| F4.10 | SLA sweepers + operational queues | M | COMPLETE | `preauth-info-request/sweeper.ts` (`e0bdccb`) — `sweepOverdueInfoRequests` (idempotent: awaiting-provider + past-due → HIGH outbox reminder, deduped per request/day; no PA event, no mutation) + `overdueInfoRequests` scoped queue read. Closes dueAt(F4.1)→breach→reminder(F4.8/F4.9). Notification delivery = `NotificationOutboxService.dispatch`. Real-DB test (1). No worker provisioned (plain service for a future cron) |

**Phase F4 COMPLETE (2026-07-24, tip after `e0bdccb`)** — the clinical-information-request rail end-to-end. **Lifecycle:** schema+catalog (F4.1) → reviewer open/cancel (F4.2) → provider response (F4.3) → reviewer accept/reopen/close (F4.4) → sanctioned-reprocessing read (F4.5, *user decision: mark-sanctioned/human-re-decides, no auto pipeline re-run*). **Read/UI:** provider inbox projection (F4.6) + inbox list/detail pages with respond form (F4.7). **Notifications:** transactional outbox + pluggable dispatcher (F4.8, *email deferred — worker unprovisioned*) ← info-request family enqueues in-tx (F4.9) ← SLA sweeper reminds overdue, deduped (F4.10). **CAVEATS (flagged per package):** built from the board's one-line rows (detailed plan not in-repo); F4.1/F4.8 added ADDITIVE schema (pushed to the throwaway PG; prod applies on next build's `prisma db push`); every service/projection is REAL-DB tested on the throwaway PG, but the F4.7 pages are NOT browser-verified (worktree env). **Two gaps flagged:** (a) reviewer admin UI to invoke F4.2/F4.4 (actions exist, need wiring on the admin PA detail); (b) reviewer-directed notification family (submitResponse) deferred. Suite 1291 pass/210 skip; tsc+brand+currency green. **Next: F5** (claim withdrawal/correction/resubmission/reconsideration) — 17 packages.

**Phase F5 COMPLETE (2026-07-25, tip after `79af013`)** — the provider claim-lifecycle rail end-to-end, all 17 packages. **Lifecycle spine:** compile-forced TRANSITIONS graph + `assertClaimTransition` (F5.1) → chain fields chainRoot/supersedes/submissionType (F5.2) → WITHDRAWN/SUPERSEDED terminals (F5.3). **Provider actions:** withdrawal (F5.4 policy → F5.5 service → F5.6 UI); correction/replacement of a pre-decision claim (F5.7 service → F5.8 lineage); post-decline resubmission eligibility (F5.9) → linked full resubmission via the canonical intake (F5.10 + UI). **Reconsideration (governed case, D13 — original claim NEVER mutated):** schema + reason policy (F5.11) → eligibility + submit (F5.12) → provider form/detail (F5.13) → TPA triage + info flow (F5.14) → maximum-delta calc through the canonical engine (F5.15) → **execute outcome** — uphold closes, accept creates ONE linked canonical supplemental child via the caseSystem intake, capped, one-outcome CAS (F5.16) → **legacy same-claim appeal retired** for reconsideration + architecture guard (F5.17). **CAVEATS (flagged per package):** every service/policy is REAL-DB tested on the throwaway PG (54329/pnos_uat), but the provider/admin UI (F5.6/F5.13 + the removed F5.17 form) is NOT browser-verified (worktree has no .env/seed) — headless + unit proven, visual deferred. **Two deferrals flagged:** (a) the reviewer ADMIN UI + server actions to invoke the F5.14/F5.16 reviewer methods (triage/assign/request/outcome — the service is the tested substance; audit tokens land with the actions); (b) the child's settlement (approval-matrix/benefit/GL/fund) runs through the canonical pipeline, not re-implemented (COA-less harness can't E2E GL/fund) — F5.16 proves ONE idempotent capped child, Σ=award; (c) the legacy-appeal DATA migration is sign-off-gated (F5.17). Additive schema (F5.2/F5.3/F5.11) pushed to the throwaway PG; prod applies on next build's `prisma db push`. Suite 1372 pass/289 skip; mutation-guard + audit-coverage + tsc + brand + currency green. **Next: F6** (remittance, disbursement, payment queries) — 12 packages; F6.1 is finance-sign-off-gated.

## F5 — Claim withdrawal, correction, resubmission, reconsideration

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F5.1 | Characterize every claim-status consumer | M | COMPLETE | `CLAIM_STATUS_CONSUMERS.md` (`fef86a6`) — read-only inventory of all `Claim.status` consumers (23 files): transition authorities, queues, **terminal-status assumptions (highest-risk group)**, APPEAL* legacy (APPEAL_APPROVED/DECLINED UNREACHABLE), reports/exclusions, money coupling, exhaustiveness map. **Key constraint: `claim-lifecycle.ts:TRANSITIONS` (Record⇒compile-forced) + `claim-status-mutation-guard.test.ts` ALLOWLIST** gate every new status-writer. Includes an F5 threading checklist + flagged supersession-vs-mutate fork (confirm F5.2/F5.3) |
| F5.2 | Claim submission-chain schema | M | COMPLETE | Claim lineage fields (`submissionType` + `chainRootClaimId`/`supersedesClaimId`/`supersededByClaimId`/`supersededAt`) + `ClaimSubmissionType` enum (`fc063c2`) — supersession model (new linked claim, not in-place mutation). `ClaimSubmissionChainService.getChain(scope,id)` resolves the full chain from either end, scoped/non-enum. Additive schema (throwaway PG; prod via build; does NOT touch guarded `status`). Real-DB test (2). Populated by F5.4/F5.7 |
| F5.3 | Lifecycle: withdrawal/supersession terminal | M | COMPLETE | `ClaimStatus += WITHDRAWN, SUPERSEDED` (terminal) (`538e5f6`) — reachable ONLY from pre-decision states (never post-decision; DECLINED stays DECLINED). Threaded ALL money/logic consumers per F5.1: TRANSITIONS (compile-forced), report-exclusions FULLY_DECLINED, dup-detection notIn (critical for F5.10), PA-attach block, editable/decided/decidable gates, STATUSES dropdown. NO status-writer (F5.5/F5.7 write them → mutation guard untouched). Lifecycle test (+1). Additive schema |
| F5.4 | Create/backfill original chains (per batch) | S/batch | COMPLETE | Self-root every claim (`chainRootClaimId=id`) (`91517ba`): persist.ts self-roots new claims in-tx (not a status write — guard green); `backfillOriginalChains` (batched, idempotent, dry-run, tenant-scoped) + `scripts/pnos-backfill-claim-chains.ts` for pre-F5.4 rows. Real-DB test (1) + persist mock fix. One-time migration |
| F5.5 | Simple provider withdrawal service | M | COMPLETE | `claim-withdrawal/{catalog,service}.ts` — FIRST F5 status-WRITER. Entitled provider (F1.3 ctx: `provider.claim.withdraw` + provider-owned + branch) idempotently withdraws an UNDECIDED claim → terminal `WITHDRAWN` via `assertClaimTransition` + a **status-guarded CAS** (`updateMany WHERE status IN {INCURRED,RECEIVED,CAPTURED,UNDER_REVIEW}`) so a decision + a withdrawal can NEVER both take effect. ZERO money/hold mutation (pre-decision ⇒ nothing to reverse). AdjudicationLog + hash-chain audit + F4.8 outbox `CLAIM_WITHDRAWN`. Added to mutation-guard ALLOWLIST; factory teardown clears AdjudicationLog. Real-DB test (21): allowed states, decided/terminal/financial denial, authz (perm/provider/branch), invalid reason, idempotent replay, **concurrency (both commit orders + concurrent double-withdraw + withdraw-vs-decision race ×6)**, zero-money. Suite 1292 pass/234 skip; tsc+brand+currency green. **Residual flagged:** decide() human path re-reads status in-tx only when `expectedRevision` set → a true-concurrent human decide could overwrite a mid-tx withdrawal (accrual not payment; reversible) — hand to F11.2/decide-hardening |
| F5.6 | Provider withdrawal UI | S | COMPLETE | Claim-detail guarded withdrawal. `claim-withdrawal/policy.ts` (pure `providerCanWithdraw` = the exact F5.5 predicate + `CLAIM_WITHDRAWABLE_STATUSES`, extracted so the page computes the **server-computed allowed action** without the service graph). `claims/[id]/page.tsx` renders `<WithdrawClaimButton>` ONLY when `providerCanWithdraw(ctx, claim)`. `WithdrawClaimButton.tsx` = accessible `role=alertdialog` (aria-modal/labelled/described) stating the permanent immutable-history consequence, required catalog reason, confirm disabled until reason chosen + while pending (double-click safe), Escape/backdrop cancel; on success closes+`router.refresh()`, on stale/decided error shows the server message + refreshes. `claims/[id]/actions.ts` = thin `withdrawProviderClaimAction` → F5.5 service (self-authorizes), maps errors, `refresh` flag for stale. Audit token added to the PR-020 catalogue (audited inside the service). Tests 18: policy 6 (hidden authorization) + action 6 (delegation/gate/stale-refresh) + component 6 (dialog a11y/keyboard/double-click/stale). Suite 1310 pass/234 skip; tsc+brand+currency green. Visual check deferred (worktree has no .env/seeded provider session — engagement convention), a11y+behavior headlessly proven via testing-library |
| F5.7 | Atomic claim replacement service | L | COMPLETE | `claim-replacement/service.ts` — FIRST SUPERSEDED writer. A provider correction creates ONE canonical linked child THROUGH the Claims Autopilot intake (`ClaimIntakeService.submitWithinTransaction`, D5 — no 2nd engine) and atomically supersedes the predecessor. Member/provider/branch DERIVED from the predecessor (chain invariant); FULL claim input validated by the ONE canonical schema (not a patch). **Explicit `replacementOfClaimRef` + NO reused invoice ⇒ null strong fingerprint ⇒ a new linked claim, never a strong-link/conflict** (autopilot §665). Status-guarded **CAS supersedes the predecessor FIRST** inside the intake tx (RECEIVED/CAPTURED/UNDER_REVIEW → SUPERSEDED via `assertClaimTransition`) so two concurrent corrections ⇒ exactly ONE current child + loser leaves predecessor active; claim-number/serialization retry. Chain wired (child `supersedesClaimId`+`chainRootClaimId`+`submissionType=CORRECTION`; predecessor `supersededByClaimId`+`supersededAt`). Idempotent replay returns the same child; changed-payload ⇒ CONFLICT. Sanctioned lineage = predecessor SUPERSEDED (F5.3 dup-exclusion), NOT a child bypass. Zero money on the predecessor. Post-commit hash-chain audit `CLAIM:REPLACE` + outbox `CLAIM_CORRECTED`. Allowlisted; factory teardown now tenant-scopes claims + clears lines/runs/receipts (intake-created children). Real-DB test 8 (lineage/immutability, structural-fail-leaves-active, decided-denied, concurrency=one-child, replay+conflict, no-bypass, zero-money, authz). Suite 1310 pass/242 skip; tsc+brand+currency green |
| F5.8 | Correction form + lineage UI | M | COMPLETE | Provider claim-detail "Correct claim" flow + submission-chain lineage. `claim-replacement/policy.ts` (pure `providerCanCorrect` = the exact F5.7 predicate — not-already-superseded + supersedable status + branch + no money + strict `provider.claim.correct` — + `CLAIM_SUPERSEDABLE_STATUSES`, extracted so the page computes the server-computed allowed action; service re-exports for compat). `claims/[id]/correct/{page.tsx (builds a SAFE prefill DTO from the predecessor, guards via providerCanCorrect else redirect), CorrectClaimForm.tsx (pre-filled; member+branch READ-ONLY/disabled ⇒ a correction can never re-identify the claim; full editable content; required confirmation checkbox; new draft-UUID idempotency; double-submit safe; stale→refresh), actions.ts (correctProviderClaimAction → F5.7 service passing ONLY content — no member/provider/branch; friendly perm gate; redirect to child; refresh on stale)}`. `claims/[id]/ClaimLineageTable.tsx` (accessible `<table>`+`<caption>` differences summary — every version oldest-first, billed Δ, superseded vs current, aria-current) wired onto the detail page via F5.2 `getChain`; "Correct claim" link gated by `providerCanCorrect`. Audit token added to the PR-020 catalogue. Tests 16: policy 6 (hidden authorization) + action 4 (delegation, NO identity fields, gate, stale-refresh) + form 3 (prefill/locked-identity, confirm+double-submit, error) + lineage 3 (accessible differences, singleton→null). Suite 1326 pass/242 skip; tsc+brand+currency green. Visual check deferred (worktree env — F3.7-F3.14 convention); behavior proven headlessly |
| F5.9 | Resubmission eligibility service | S | COMPLETE | `claim-resubmission/{policy.ts,eligibility.service.ts}` — READ-ONLY (no writes/status mutation). `ClaimResubmissionEligibilityService.check(ctx, claimId, at?)` computes WHETHER/WHY/UNTIL-WHEN a DECLINED claim may be resubmitted. Pure `resolveResubmissionReason` sources `resubmissionAllowed` + a **SAFE** reason from the canonical `AdjudicationReasonCode` catalog (line-level `reasonCodeId` → catalog, then claim-level decline code → catalog) and falls back to a legacy `Claim.declineReasonCode` map that **NEVER discloses internal/fraud text** (FRAUD_SUSPECTED ⇒ "declined after review — contact the payer"). Pure `resubmissionDeadline` from `ProviderContract.submissionWindowDays`+`submissionWindowBasis` (SERVICE/DISCHARGE/INVOICE/MONTHLY_BATCH), computed in **UTC, deadline-day inclusive** (timezone-safe). Codes: ELIGIBLE / NOT_FOUND (non-enum cross-provider) / FORBIDDEN (perm+branch) / NOT_DECLINED / ALREADY_RESUBMITTED (supersededBy or a successor with supersedesClaimId — current-chain scope) / REASON_NOT_RESUBMITTABLE / DEADLINE_PASSED. Perm = `provider.claim.correct` (no dedicated resubmit perm — flagged). Step-4 doc-response no-op (no claim-side info-request model; DECLINED carries none). Real-DB test 8 + pure test 10 (reason allowed/forbidden, fraud-never-disclosed, boundary-timezone, already-resubmitted, cross-provider, not-declined, perm/branch). Suite 1336 pass/250 skip; tsc+brand+currency green. Read-only ⇒ no mutation-guard/audit-token needed |
| F5.10 | Linked post-decline resubmission | M | COMPLETE | `claim-resubmission/submit.service.ts` — an ELIGIBLE (F5.9) decline produces a FULL new RESUBMISSION through the Claims Autopilot intake (`submitWithinTransaction`); the original **stays DECLINED** (decision+money immutable) while the chain-head pointer advances. Reuses the F5.7 "replacement full-form contract" (extracted `claim-replacement/submission.ts` = `ReplaceClaimCommand`+`buildReplacementSubmission`+retry helpers; F5.7 refactored to consume it). Idempotency (reserveReceipt) BEFORE F5.9 eligibility ⇒ replay returns the child; the chain pointer is claimed with a **CAS on `supersededByClaimId IS NULL` (NO status write ⇒ no mutation-guard trip)** ⇒ 2 concurrent ⇒ exactly ONE child. Chain wired (child.supersedesClaimId/chainRoot/submissionType=RESUBMISSION). NO inheritance of pricing/approval (fresh RECEIVED + PENDING run). Audit `CLAIM:RESUBMIT` + outbox `CLAIM_RESUBMITTED`. **UI:** generalized `CorrectClaimForm` (backward-compat `mode`+`submitAction` props) reused by `claims/[id]/resubmit/{page.tsx (F5.9-gated prefill),actions.ts}`; gated "Resubmit" entry on the DECLINED detail; lineage already shows RESUBMISSION via F5.8. Audit token added. Test 9: service 5 (immutable-original+no-inheritance, ineligible-denied, not-declined, replay, concurrency=one-child) + action 4. Suite 1340 pass/255 skip; F5-lifecycle DB batch 47/47; tsc+brand+currency green. Visual deferred (worktree env) |
| F5.11 | Reconsideration schema + reason policy | M | COMPLETE | **ADDITIVE schema** (pushed to throwaway PG 54329; prod applies on next build's `prisma db push`): `ClaimReconsideration` (§7.8 — tenant/client/provider/branch, claimId+chainRootClaimId relation-less, reasonCode, providerNarrative, requestedAmount+currency, filingDeadline/filedAt, `ReconsiderationStatus` enum [11: DRAFT→CLOSED], assignedTeam/reviewer, originalAdjudicatorId [SoD], outcome safe-vs-internal split, supplementalClaimId, SLA fields, idempotencyKey+version), `ClaimReconsiderationLine` (claimLineId relation-less; frozen original billed/allowed/payable/memberShare/writeoff + requested/reviewerCorrectedEntitlement + alreadyApproved/Paid + maxIncrement + awardedIncrement, all `@db.Decimal(14,2)`), `ClaimReconsiderationEvent` (mirrors PreAuthorizationEvent: safeReasonCode vs internalReasonRef, message, seq-unique). Relation-less to Claim/ClaimLine (F3.2/F5.2 satellite convention); evidence via existing DocumentUploadIntent targetType RECONSIDERATION (no Document change). **Pure `claim-reconsideration/policy.ts`:** reason catalog + eligibility (by decision/category), deadline resolution order (contract→client→default 60d, UTC day-inclusive), decimal invariants (`reconsiderationMaxIncrement`=corrected−max(approved,paid)≥0; `validateAwardedIncrement` 0≤awarded≤max; `sumAwardedIncrements`), consistency (`lineBelongsToClaim`, `providerAndCurrencyConsistent`), safe/internal `toProviderReconsiderationProjection`. Factory teardown clears the 3 satellites. Test 11 (policy 10 + schema round-trip 1). Suite 1350 pass/256 skip; factory DB batch 46/46; tsc+brand+currency green. No service (stop); no status write ⇒ no mutation-guard/audit-token. **D13: reconsideration = governed case, original claim UNTOUCHED** |
| F5.12 | Reconsideration eligibility + submit | M | COMPLETE | `claim-reconsideration/submit.service.ts` — `ClaimReconsiderationService.{checkEligibility, submit}`. A provider files ONE governed reconsideration on a DECIDED claim; **NO claim write (D13 — original status/money/benefit/GL/fund UNTOUCHED) ⇒ no mutation-guard.** Eligibility reuses F5.11 policy (RECONSIDERABLE_CLAIM_STATUSES, reason-by-decision, deadline from decidedAt). `submit`: perm `provider.claim.reconsider` + provider/branch scope → **idempotent replay by idempotencyKey (before dup check)** → eligibility (decided/reason/deadline/one-active) → validate (narrative, requestedAmount>0, line targets belong to claim, clean evidence [scanStatus not PENDING/REJECTED/QUARANTINED/ERROR]) → create case (SUBMITTED, filingDeadline, currency+adjudicatorId derived from claim, SLA dueAt) + **exact frozen line snapshots** (originalBilled/allowed/payable/memberShare/writeoff from ClaimLine; maxIncrement/awarded=0 until F5.15) + first SUBMITTED event, all in one create; `@@unique[tenant,key]` P2002 → replay. Post-commit hash-chain audit `RECONSIDERATION:SUBMIT` + outbox `RECONSIDERATION_SUBMITTED`. `checkEligibility(ctx,claimId,{reasonCode?,at?})` for F5.13 gating (ELIGIBLE/NOT_FOUND/FORBIDDEN/NOT_RECONSIDERABLE/DEADLINE_PASSED/REASON_NOT_ELIGIBLE/ALREADY_ACTIVE). One-active per claim (TERMINAL=UPHELD/WITHDRAWN/CLOSED free it). Real-DB test 7 (create+exact-snapshot+claim-untouched, all-decided-states, not-reconsiderable, wrong-reason/wrong-line/expired, idempotent+one-active, permission, checkEligibility gate). Suite 1350 pass/263 skip; tsc+brand+currency green. No review/outcome (stop) |
| F5.13 | Provider reconsideration form/detail | M | COMPLETE | Provider reconsideration UI. `claims/[id]/reconsider/{page.tsx (F5.12-checkEligibility-gated; loads claim+lines with FROZEN original amounts+safe declineReason), ReconsiderClaimForm.tsx, actions.ts}` + `ReconsiderationPanel.tsx` (detail) + gated "Reconsider" entry on the DECIDED claim detail. Form: accessible `<table>`+`<caption>` of lines (checkbox dispute + frozen billed/allowed + safe reason + requested-corrected input); **EXACT total delta = Σ max(0, requested−allowed)**; reason picker (F5.11 `reconsiderationReasonsFor(status)` + safe description) + narrative + **required declaration**; filing deadline shown; submits via F5.12 (which RE-checks eligibility ⇒ a stale form is refused → error+refresh). Panel: renders ONLY the F5.11 `toProviderReconsiderationProjection` (status/requested/deadline/dueAt/outcome/own-narrative) ⇒ no internal leak. Detail page loads the latest case + eligibility (only for reconsiderable claims). Audit token `ClaimReconsiderationService.submit(` added. Evidence upload DEFERRED (F2 flow; optional in F5.12). Test 10 (action 4 + form 4 [frozen amounts/accessible table, exact decimals, required-gates, stale] + panel 2 [safe render, no internal leak]). Suite 1360 pass/263 skip; tsc+brand+currency green. Visual deferred (worktree env); headless proven. No TPA decision (stop) |
| F5.14 | TPA reconsideration triage + info flow | M | COMPLETE | The reviewer side of the case. `claim-reconsideration/events.ts` (`appendReconsiderationEvent` — append-only, per-case seq=max+1, unique(reconsiderationId,sequence) fails a double-append loudly; reuses `assertSafeEventMetadata`; `message`=SAFE exchange text vs `internalReasonRef`=pointer) + `claim-reconsideration/review.service.ts` (`ReconsiderationReviewService`): **triage** (SUBMITTED→TRIAGE); **assign** (→UNDER_REVIEW) with the **SoD RULE** — assigning `originalAdjudicatorId` is REFUSED unless `acknowledgeSelfReview`, and the returned `sodWarning` drives a UI banner; **requestInformation** (→INFORMATION_REQUIRED, safe prompt + safe outbox `RECONSIDERATION_INFO_REQUESTED`); **respondToInformation** (provider-authorised via ProviderAccessContext, INFORMATION_REQUIRED→PROVIDER_RESPONDED, **non-enumerating** NOT_FOUND on scope miss); **resumeReview** (→UNDER_REVIEW); **addInternalNote** (INTERNAL_NOTE event kept OUT of provider state, §9, no status/version change); **queue** (SLA-ordered staff work list). Every mutation = a status- AND version-guarded CAS (`updateMany WHERE version+status`) ⇒ stale/out-of-state refused (STALE/INVALID_STATE), never a silent overwrite; event + hash-chain audit follow. Role re-asserted (`RECONSIDERATION_REVIEWER_ROLES`) behind the action's `requireRole(ROLES.CLINICAL)` — no next-auth import. `policy.ts` + `toProviderReconsiderationTimeline` — **"provider sees only shared state"** by construction: internal events (TRIAGED/ASSIGNED/UNDER_REVIEW/INTERNAL_NOTE) dropped + `message` surfaced ONLY for INFO_REQUESTED/PROVIDER_RESPONDED; internalReasonRef/actor/metadata never carried. **NO financial outcome (D13 — claim untouched); F5.15/F5.16 own corrected-entitlement/award/uphold.** Test 11 (timeline pure 4 + reviewer real-DB 7: role gate+triage→assign, SoD block/allow-with-ack, stale+wrong-state, request→respond→resume ordered events, owner-only+pending-only response, internal-activity-absent+claim-untouched, queue+role gate). Suite 1364 pass/270 skip; reviewer DB 7/7 on throwaway PG; tsc+brand+currency+audit-coverage green. No new schema (reuses F5.11). **Deferred (flagged):** reviewer ADMIN UI + actions (F4 precedent — service is the tested substance; audit token lands with the actions); per-LINE review drafts → F5.15/F5.16 |
| F5.15 | Reconsideration maximum-delta calculation | M | COMPLETE | `claim-reconsideration/calculation.service.ts` — `ReconsiderationCalculationService.computeMaxDelta(actor, id, {reprice?})`, **READ-ONLY** (no money/status/line write; D13). Per disputed line: (1) load the case + disputed lines + the claim's **LIVE approved amounts** (prior base) + paid status, tenant-scoped; (2) **sum prior ACCEPTED supplemental awards** (`awardedIncrement`) across every OTHER reconsideration on the claim ⇒ **no double-allowance** (§7.8); (3) **re-price via a REPRICER PORT defaulting to `ContractEngine.evaluateClaimById`** (the CANONICAL pricer, never copied) at the claim's governing service date — a PENDED line or unmatched claim ⇒ **non-deterministic**, no delta, "reviewer judgment"; (4) `correctedEntitlement`=engine payable, `maxIncrement`=F5.11 `reconsiderationMaxIncrement` (max(0, corrected − max(priorApproved, priorPaid))); (5) capture `contractVersionId` + a safe explanation. **All money decimal.js, HALF_UP 2dp** (no float drift). Port is **injectable** ⇒ the calc math is tested deterministically without a tariff fixture, while production calls the real engine (proven by a default-port test that handles the unmatched case). Reviewer-gated (`RECONSIDERATION_REVIEWER_ROLES`). Engine evaluate path verified read-only. Test 10 (opt-in DB): partial/underpaid + total, declined-from-zero, PAID→max(approved,paid), prior-award-summed (900−(600+200)=100 not 300), tariff-below-prior→0, fractional 700.10−600.05=100.05, pended+unmatched non-deterministic, deterministic replay, role gate + claim-untouched, default-port-calls-engine. Suite 1364 pass/280 skip; calc DB 10/10 on throwaway PG; tsc+brand+currency+audit-coverage green. No new schema; read-only ⇒ no mutation-guard/audit-token. **STOP: no outcome execution (F5.16)** |
| F5.16 | Execute reconsideration outcome | L | COMPLETE | `claim-reconsideration/outcome.service.ts` — `ReconsiderationOutcomeService.execute(actor, id, decision, {reprice?})`, the case's FIRST money path. **UPHELD/zero → close, NO child; ACCEPTED/PARTIALLY_ACCEPTED → ONE linked canonical child** (submissionType RECONSIDERATION) billing ONLY the awarded positive per-line deltas. **ORIGINAL claim never touched (D13).** Flow: reviewer-gated + reason/explanation required → replay (terminal case returns its result, idempotent) → **HARD-CAP each award to the F5.15 max** (`computeMaxDelta`, prior accepted awards subtracted) → disposition↔award consistency → build child from the ORIGINAL + awarded deltas + **reserve idempotent receipt (`recon-outcome:<id>`) BEFORE the tx** → **ATOMIC**: status+version-guarded CAS on the case (**one-outcome ⇒ concurrent double-outcome = exactly ONE child**), freeze per-line awardedIncrement/maxIncrement/corrected, create child via the **CANONICAL Claims Autopilot intake** (D5 — `caseSystem` caller, CASE_FINAL, `submitWithinTransaction`), stamp submissionType+chainRootClaimId, link supplementalClaimId, append outcome event → audit + safe outbox AFTER commit. Claim-number/serialization collision retries the tx; STALE/INVALID_STATE not retried. **Child created RECEIVED — settlement (approval-matrix/benefit/GL/fund) is the canonical pipeline's exactly-once concern, NOT re-implemented (mirrors F5.10).** Repricer injectable ⇒ accepted paths testable without a tariff fixture (intake still creates a real child). Test 9 (opt-in DB): ACCEPT one-child+capped+D13-untouched, UPHOLD/zero no-child, Σ child lines=award, above-max refused, prior-supplemental subtracts, **concurrent double-outcome=1 child**, replay no-2nd-child, build-failure recoverable (case UNDER_REVIEW @ same version, no orphan), role+version gate. Suite 1364 pass/289 skip; outcome DB 9/9 on throwaway PG; **mutation-guard green (NO Claim.status write — original immutable, child RECEIVED via allowlisted intake)**; audit-coverage green (service, no action); tsc+brand+currency green. No new schema. **Deferred (flagged):** child settlement + matrix approval run via the canonical pipeline (COA-less harness can't E2E GL/fund — proven at this level: ONE child, idempotent, Σ=award, capped); reviewer ADMIN UI (with F5.14). **STOP: no legacy appeal migration (F5.17)** |
| F5.17 | Consolidate legacy appeal semantics | M | COMPLETE | Same-claim appeals (which flipped the ORIGINAL claim to APPEALED) RETIRED for reconsideration (F5.11-16, no claim mutation, D13). `claim-adjudication.service.ts::initiateAppeal` now **throws PRECONDITION_FAILED** (no `status:"APPEALED"` write); `initiateAppealAction` + the admin "Initiate Appeal" form + `canAppeal` **removed** (historic APPEALED badge + audit untouched); `claim-lifecycle.ts` →APPEALED edges annotated RETIRED + KEPT (no deletion — historic validity). New pure `claim-reconsideration/legacy-appeal.ts`: `LEGACY_APPEAL_TO_RECONSIDERATION` (APPEALED→UNDER_REVIEW, APPEAL_APPROVED→ACCEPTED, APPEAL_DECLINED→UPHELD) + `mapLegacyAppealToReconsideration` (reasonCode LEGACY_APPEAL, facts preserved, migratable ONLY w/ notes+distinct-reviewer; NEVER touches a claim). Audit-coverage catalogue: dropped the stale initiateAppealAction entry. **Architecture guard test (8, pure):** no source writes status APPEALED/APPEAL_APPROVED/APPEAL_DECLINED (comment-aware scan), nothing invokes initiateAppeal, mapping preserves facts + returns null for non-appeals, migratable-predicate, enum values still exist. **Confirmed: APPEAL_APPROVED/DECLINED had NO status writer (dead targets); only APPEALED was written, now retired.** Doc `LEGACY_APPEAL_CONSOLIDATION.md` (inventory+mapping+guard+gated migration). Suite 1372 pass/289 skip; audit-coverage+mutation-guard+tsc+brand+currency green. No schema change. **Deferred (flagged):** the DATA migration (create cases for migratable legacy appeals) gated on product/claims/finance sign-off + prod dry-run (likely 0 rows); adds cases only, never edits a claim ⇒ totals/decisions unchanged. **STOP: no deletion of legacy statuses** |

## F6 — Remittance, disbursement, payment queries

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F6.1 | Frozen remittance formula + field dictionary | S | SPEC COMPLETE · GATED(finance sign-off) | `REMITTANCE_FIELD_DICTIONARY.md` — finance-approval-gated spec (NO code, NO schema, NO data) mapping **every** provider-visible settlement amount to a real stored source fact (`schema.prisma`/service line-cited on this branch). **Central finding: money flows through TWO independent, unreconciled tracks** — Track A *decision* (`Claim.approvedAmount`→`paidAmount`, claim aggregates, per-line `ClaimLine.approvedAmount`; **settlement-authoritative**) and Track B *contract-engine provenance* (per-line `contractedAmount/disallowedAmount/shortfallAmount/providerWriteOff/payerLiability/memberLiability`, `persist.ts` provenance-only, never overwrites `approvedAmount`) which **can diverge** ⇒ **rule R-1**: claim header authoritative for money, line breakdown indicative, per-claim residual surfaced. Field dictionary (batch/voucher/claim/line, each row = provider label · definition · source fact@line · track · stored/derived · precision · provider-safe?); currency axes (txn vs base, single-currency Phase-1 refused-if-mixed) + Decimal HALF_UP-2dp boundary rounding; supplemental/reconsideration lineage display (submissionType/chainRoot/supplementalClaimId/awardedIncrement, I4 ceiling, D13); provider-safe reason mapping (`ReasonCodeService.resolve`→`AdjudicationReasonCode.providerDescription/remedy/resubmissionAllowed`, severity→money, FRAUD_SUSPECTED neutralised, NEVER internalDescription/declineNotes/free-text); batch/voucher/disbursement/GL formulas with **I5** (Σ line payable=Σ claim payable=batch total=voucher=paid `= successful disbursement [MISSING]`) + **I6** (base/GL Dr2010/Cr1010, independent axis); **12-entry data-gap register** (D-1 track divergence, D-3 dead `excessAmount`, D-4 shortfall vs writeoff overlap, D-5 no line paid, D-6 no line base, **D-7 ProviderDisbursement MISSING** (D16), D-8 65,30 vs 14,2 precision, D-10 human-decline null reasonCodeId…) each with closing package + finance-decision flag; **6 worked examples** (full/partial-writeoff/decline-exclusion/multi-line-costshare/supplemental/multi-currency) as F6.2/F11.3 fixtures; **§12 sign-off block (Q1-Q5) left PENDING** — claims+network+finance must ratify before `providerRemittanceV2` reads to any provider. Feeds F6.2. **STOP: no code** |
| F6.2 | Canonical ProviderRemittanceService | M | COMPLETE (stage-1 internal evidence; provider read still §12-gated) | `provider-remittance/{projection.ts (pure),service.ts}` — the ONE scoped frozen read model (F6.1 dictionary). `getBatchRemittance(ctx, batchId, {page,pageSize})`: (1) `ProviderAccessService.requirePermission('provider.settlement.read')`; (2) load batch **provider-scoped + non-enumerating** (`findFirst {id,tenantId,providerId}` → NOT_FOUND, identical to absent, §9.1); (3) project §4 (batch/voucher/claim/line) from **STORED snapshots only — NO engine/FX recompute** (D15); (4) voucher via scalar join (NO journalEntryId); (5) **conservation over the WHOLE batch via aggregates** (invariant across pages) — I5 legs (lineToHeader/headerToBatch/batchToVoucher/paidToApproved) + I6 base legs, **decimal.js HALF_UP-2dp** boundary, **R-1 per-claim residual** (D-1: header authoritative, `lineResidual`/`linesReconciled` surfaced not hidden), `disbursementLeg:"MISSING"` (D-7); (6) deterministic pagination (`orderBy [claimNumber,id]`); (7) **excludes every Safe?=N field** by construction (no maker/checker/notes/journalEntryId/internalDescription/declineNotes/ruleTrace/contractedRate). Reuses F5.9 `LEGACY_DECLINE_RESUBMISSION` for claim-decline safe text (FRAUD_SUSPECTED neutralised) + line `reasonCode` relation (providerDescription/remedy/severity, NEVER internalDescription, §7). Track A `approvedAmount` = authoritative payable; Track B `payerLiability` shown as labelled provenance. + `listBatches(ctx)` provider-scoped list read (for F6.4). Factory extended: `createSettlementBatch` (frozen SETTLED batch+voucher+PAID claims+lines+reason codes, direct insert — COA-less harness) + teardown for batch/voucher/reason rows. **NO schema change; NO status write (mutation-guard N/A); NO server action (audit-coverage N/A); read-only (no money mutated).** Tests: pure projection/conservation 20 (6 F6.1 worked examples, D-1 divergence, D-7 note, safe-reason, HALF_UP) + opt-in DB 10 (E4 conserve I5/I6, **no-recompute [shows frozen approvedAmount not engine payerLiability], cross-provider + cross-tenant NOT_FOUND, permission denial, provider-safe snapshot, pagination totals reconcile, D-1 service-level, listBatches isolation**). Suite 1392 pass/299 skip; DB 10/10 on throwaway PG; tsc+brand+currency green. **STOP: no page/export.** Provider-facing read stays OFF until F6.1 §12 finance sign-off (providerRemittanceV2, §11.1) — this is §11.6 stage-1 internal evidence. Next: F6.3 (migrate admin settlement detail onto this read model). |
| F6.3 | Migrate admin settlement detail to service | S | COMPLETE | Admin settlement detail `src/app/(admin)/settlement/[id]/page.tsx` now consumes the canonical read model instead of duplicating batch/claim/voucher selects + arithmetic. **Extracted a shared `assembleCore` in `provider-remittance/service.ts`** so the provider entry (`getBatchRemittance`) and the new operator entry (`getBatchRemittanceForOperator`) use IDENTICAL projection + conservation (can never drift). **`getBatchRemittanceForOperator(scope, batchId, opts)`**: tenant-scoped (an operator views ANY provider's batch in-tenant — settlement is provider-level not client-level ⇒ no client confinement), caller-role-gated (page keeps `requireRole(FINANCE)`), returns **null → notFound** (trusted internal, not the provider entry's throw), and returns the SAME provider-safe model **PLUS `admin` extension** (the F6.1 Safe?=N fields: `maker`/`checker` {id,name}, `notes`, `provider` contact, GL `journalEntry` {entryNumber,entryDate,description}). **Provider entry UNCHANGED — never carries `admin`** (separation proven by test). Page fixes: sources every field from the read model (member/billed/approved/voucher/journal), uses **`batch.currency`** (was hardcoded `UGX`), adds a finance **conservation badge** (lines=claims=batch=voucher ✓) + maker/checker names + a "first N of M" note (MAX_PAGE_SIZE 200→1000 so a statement renders every claim; no silent truncation). Factory: `createSettlementBatch` gained `withJournal`/`notes` (+ JournalEntry teardown). Tests +6 opt-in DB (operator admin extension incl. journalEntry; **parity vs a direct query — no lost fields**; operator sees any in-tenant provider's batch; **cross-tenant ⇒ null**; unknown ⇒ null; provider entry has no `admin`). Suite 1392 pass/305 skip; DB 16/16; tsc+brand+currency+audit-coverage green. **NO schema change; page is a read (no server action ⇒ audit-coverage N/A; no status write).** Admin page visual verification deferred (worktree has no .env/seeded admin session — F3.x–F5.x convention); parity + tsc prove the migration. Next: F6.4 (provider settlement detail page — the FIRST provider-facing remittance surface, §12-gated). |
| F6.4 | Provider settlement detail page | M | COMPLETE (behind providerRemittanceV2, default OFF — §12-gated) | `src/app/provider/settlements/[id]/page.tsx` — the FIRST provider-facing remittance surface (§8.9), rendering ONLY the provider-safe `getBatchRemittance` model (no admin/GL/internal/bank fields exist on that shape). **Gated behind `providerRemittanceV2` (§11.1): OFF by default ⇒ the route 404s for a provider until the F6.1 §12 finance sign-off flips the tenant/provider on.** Auth authority = the F6.2 service itself (strict `provider.settlement.read` + provider scope, non-enumerating); the page translates `FORBIDDEN_PERMISSION`→/unauthorized, `NOT_FOUND`→notFound. Renders: batch identity/status/settledAt; summary cards (total payable, voucher, **honest "payment facts not yet recorded" per D-7/D16**); a `role=status` conservation strip (lines=claims=batch=voucher ✓/⚠); per-claim sections (claim# → `/provider/claims/[id]`, member, service date, **supplemental lineage badge + link to original**, approved/paid, safe claim decline reason) each with an **accessible responsive lines table** (`<caption class=sr-only>`, `th scope=col/row`, `overflow-x-auto min-w`) showing billed/allowed/disallowed/member/write-off/approved/paid + safe line reason (remedy in `title`); per-claim residual note when lines≠header (R-1); "first N of M" note. **New flag in `provider-access-settings.service.ts`**: `providerRemittanceV2` + `remittanceV2ProviderIds` allow-list (mirrors the F1.11 entitlement flag) + `isRemittanceV2Enabled(tenant,provider)`; DEFAULT OFF. List page `/provider/settlements` links rows to the detail ONLY when the flag is on (legacy list otherwise). **Payment-query action (step 6) DEFERRED — depends F6.10/F6.11 (stop: no query submit).** Tests +7 pure (flag parse defaults+additive+`===true`-only; `isRemittanceV2Enabled` off-default/global/allow-list; entitlement mirror); updated the F1.11 parse-shape test for the additive fields. Suite 1399 pass/305 skip; tsc+brand+currency+audit-coverage green. **NO schema; page is a READ (no server action).** Leakage/totals/reasons guarantees inherited from the F6.2 provider-safe tests (the page renders that model verbatim). Visual verification deferred (flag OFF ⇒ 404; worktree has no .env/seeded provider session — F3.x–F5.x convention); gate + service are unit-tested. Next: F6.5 (CSV remittance export from the same model). |
| F6.5 | CSV remittance export | M | COMPLETE (sync download; async/stored-artifact deferred) | Authorized CSV export deriving from the SAME read model. **Pure `provider-remittance/csv.ts`**: stable versioned column dictionary (`REMITTANCE_CSV_VERSION` 1.0 + `REMITTANCE_CSV_COLUMNS`, line-grain), **`csvCell` OWASP formula-injection guard (neutralize leading `= + - @ TAB CR` with a `'`) — which the existing app CSV exports LACK** — + RFC-4180 quoting, UTF-8 BOM (Excel Unicode), decimal totals, deterministic **sha256 checksum of the delivered bytes**. **`ProviderRemittanceService.exportBatchCsv(ctx, batchId, {pageSize})`**: `requirePermission('provider.settlement.export')` (+ reuses `getBatchRemittance` ⇒ settlement.read + provider scope + NOT_FOUND) → **pages the read model to EXHAUSTION (no omitted rows)** → `buildRemittanceCsv` → `{filename, csv, evidence}`. **Route `/provider/settlements/[id]/export/route.ts`** (thin adapter, F2.6 pattern): `resolveUserContext` → **providerRemittanceV2 flag gate** (404 off) → export → `text/csv` + `Content-Disposition` + `X-Remittance-{Row-Count,Checksum,Csv-Version}` headers + **`writeAudit('SETTLEMENT:EXPORT')` egress evidence**; FORBIDDEN→403, NOT_FOUND→404. F6.4 page gains a flag+`provider.settlement.export`-gated "Export CSV" link. Tests: pure 10 (injection leads incl. neutralize-then-quote, RFC quoting, BOM/version/columns, rowCount=lines, **totals=read model**, hostile description inert, checksum determinism + change-detect, Unicode) + DB 5 (export-perm required, totals match conservation, **tiny pageSize omits no rows**, cross-provider NOT_FOUND, unknown NOT_FOUND). Suite 1409 pass/310 skip; DB 21/21; tsc+brand+currency+audit-coverage green. **NO schema; route handler (not a server action) ⇒ audit-coverage N/A; read-only.** **DEFERRED (flagged): async generation for very large output + a stored export artifact with row-count/totals/checksum/requester/EXPIRY + short-lived signed link (spec steps 3/5/6) — the app has no async-export/job infra; the synchronous authorized download matches every existing app CSV export, evidence is stored via the audit log + response headers (no separate expiring link ⇒ the "result-link expiry" sub-test is N/A for the sync model).** Export surface gated with F6.4 (providerRemittanceV2 OFF). Next: F6.6 (PDF/print remittance from the same model). |
| F6.6 | PDF/print remittance | M | COMPLETE | Provider-safe PDF/print statement from the SAME read model, mirroring the F3.14 GOP `@react-pdf` pattern (client-rendered via a "use client" button). **Pure `remittance-pdf.ts`** (`REMITTANCE_PDF_VERSION` 1.0): `buildRemittancePdfData(remittance, {generatedAt})` flattens `getBatchRemittance` into the DTO — provider-safe by construction (only projection fields; **generatedAt is an INPUT ⇒ deterministic/testable**, not the wall clock). **`RemittanceDocument.tsx`** (`@react-pdf`): versioned template with a **`fixed` page header + footer with page numbers** (repeat across pages), currency, voucher reference, generated time, **control total** (stored batch total) + conservation ✓/⚠, and a claim/line table (billed/allowed/disallowed/member/write-off/approved/paid + safe reason sub-row) that **`wrap`s + breaks cleanly for long, multi-page detail**; each row `wrap={false}` so it never splits. **`RemittancePdfButton.tsx`** ("use client", mirrors `GopButton`): `pdf(<RemittanceDocument/>).toBlob()` → download. F6.4 page builds the DTO (server) + renders the button alongside Export CSV. **Client-render from already-authorized, provider-safe page data ⇒ NO new server egress to audit (the page load is the access event, same as GOP).** Tests +6 pure (version/cycle/currency/voucher/control-total; claim/line amounts + safe reason parity; supplemental lineage; **provider-safe — no makerId/journalEntryId/payerLiability/ruleTrace**; generatedAt deterministic/omit⇒null; honest payment-facts note). Suite 1415 pass/310 skip; tsc+brand+currency+audit-coverage green. **NO schema; client-rendered PDF (no server action/route) ⇒ audit-coverage N/A.** **DEFERRED (flagged): visual QA of short/long examples (step 5) on a seeded run — flag OFF + worktree no session (F3.x convention; @react-pdf Document tsc-verified + mirrors proven GOP); a >1000-claim batch's PDF is capped to the page's loaded set (CSV export is the complete-data path — same as F6.4).** Gated with F6.4 (providerRemittanceV2 OFF). Next: F6.7 (provider disbursement schema/state machine — the D-7/D16 gap; ADDITIVE schema). |
| F6.7 | Disbursement schema/state machine | M | COMPLETE | Closes the D-7/D16 gap: **ADDITIVE `ProviderDisbursement` model + `DisbursementStatus` enum** (§7.9) at schema EOF — actual payment facts DISTINCT from the voucher/batch (which authorize). Relation-less id pointers (satellite convention F3.2/F5.11 — no ProviderSettlementBatch/PaymentVoucher churn): tenant/provider/`settlementBatchId`/`voucherId`, status (PENDING/RELEASED/PROCESSING/SUCCEEDED/FAILED/REVERSED), `amount`+`currency`+`baseAmount`+`baseCurrency`+`fxRateRef` (frozen FX, never recompute), `method`/`channel`/`maskedDestination` (masked-only, never plaintext §9/D27)/`externalReference`/`valueDate`, initiated/confirmed/failed/reversed actors+times, **`failureReasonSafe` vs `failureReasonInternal` (separated §9)**, `reversalOfDisbursementId`, `idempotencyKey`, `reconciliationStatus`, `version`. **`@@unique([tenantId, idempotencyKey])` (nulls distinct) — NO batch-level unique** so 1:1 is normal but **split/partial disbursements are allowed (§I5)**; the key-unique prevents dup same-key records. Pushed to throwaway PG (validate, NOT format; prod applies additive on next build's `prisma db push`). **Pure `provider-disbursement/state-machine.ts`**: `DISBURSEMENT_TRANSITIONS` (PENDING→{RELEASED,FAILED}→PROCESSING→{SUCCEEDED,FAILED}; SUCCEEDED→REVERSED; **FAILED/REVERSED TERMINAL** ⇒ retry=NEW record, never silently unpays a batch) + `canTransition`/`assertDisbursementTransition` + `isSuccessfulDisbursement` (ONLY SUCCEEDED counts for the I5 successful-disbursement leg; REVERSED does not) + **`DISBURSEMENT_TRANSITION_ACTOR` maker/checker model** (RELEASED=MAKER, SUCCEEDED/REVERSED=CHECKER, FAILED=SYSTEM; F6.8 binds to FINANCE roles + actor separation). Compile-forced Record over the enum type (`import type` ⇒ runtime-free/testable). Factory: `createDisbursement` helper + teardown. Tests +7 pure (forward lifecycle, failure branches, terminals, no-skip/no-resurrect, assert throws, success-leg, maker/checker) + 4 DB (round-trip incl. masked destination + defaults; **safe-vs-internal failure reason separation**; **idempotency dup rejected P2002**; **split disbursements allowed**). Suite 1422 pass/314 skip; DB 11/11; tsc+brand+currency+audit-coverage green. **NO service (F6.8); NO paid-state change — the FG-C7 atomic mark-paid gate is UNTOUCHED (additive schema + pure module can't weaken it).** Next: F6.8 (disbursement record/confirm service — maker/checker, must not weaken the atomic gate). |
| F6.8 | Disbursement record/confirm service | M | COMPLETE | `provider-disbursement/service.ts` — finance records/releases/confirms the ACTUAL provider payment for an already accounting-SETTLED batch (§7.9/D16), driving the F6.7 state machine. **`record`** (maker, `inSerializableTx`): batch must be SETTLED + have a voucher; **currency must match the voucher**; **over-disbursement guard** (Σ live {PENDING/RELEASED/PROCESSING/SUCCEEDED} + new ≤ voucher total ⇒ split allowed, over refused); idempotent (idempotencyKey pre-check + P2002-race → returns winner); creates PENDING w/ initiatedById. **`release`/`markProcessing`** (maker), **`confirm`** (checker — requires externalReference + valueDate + **maker≠checker by actor id** ⇒ SEPARATION_OF_DUTY; the "actually paid" fact), **`fail`** (safe+internal reason separated), **`reverse`** (checker, compensating). **Every transition = role-gated (FINANCE string-literals, no rbac import) + status- AND version-guarded CAS** (F5.14 pattern: `updateMany WHERE version+status∈from`; 0 rows → re-read → NOT_FOUND/STALE/INVALID_STATE) + `assertDisbursementTransition` defence + post-commit `auditChainService.append(DISBURSEMENT:*)` + provider outbox on confirm/reverse. **FG-C7 CRITICAL: NEVER mutates batch/voucher/GL — the batch is set SETTLED ONLY by canonical `markSettlementBatchPaid` (this service requires it as a precondition, never re-implements/bypasses it, spec step 5); "actually paid" = the disbursement SUCCEEDED fact, not a new batch state.** Tests +10 opt-in DB (record on SETTLED only; non-settled/currency/over-disbursement refused; idempotent replay; full lifecycle maker≠checker + confirm-needs-ref/value-date; illegal-transition + stale-version refused; **concurrent confirm ⇒ exactly one SUCCEEDED**; fail safe-vs-internal; reverse compensates; finance-role required; **batch+voucher byte-for-byte UNCHANGED after a full record→confirm cycle**). Suite 1422 pass/324 skip; DB 10/10; tsc+brand+currency+audit-coverage+mutation-guard green. **NO schema; no server action (audit-coverage N/A — audits via auditChainService); NO batch/voucher/GL mutation.** **Deferred:** admin finance UI + server action to invoke it (F5.14 convention — service is the substance); no bank integration (stop). Next: F6.9 (settlement reconciliation job/dashboard — I5: Σ successful disbursement = batch = voucher). |
| F6.9 | Settlement reconciliation job/dashboard | M | NOT_STARTED | — |
| F6.10 | Payment-query schema/service | M | NOT_STARTED | — |
| F6.11 | Provider/finance payment-query pages | M | NOT_STARTED | — |
| F6.12 | Payment-query → reconsideration handoff | S | NOT_STARTED | — |

## F7 — Contract visibility, master data, network self-service

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F7.1 | Provider-visible contract field policy | S | GATED(network/legal/security review) | — |
| F7.2 | Provider contract/rate read service | M | NOT_STARTED | — |
| F7.3 | Contracts/rates pages + safe export | M | NOT_STARTED | — |
| F7.4 | Master-data change-request schema/service | M | NOT_STARTED | — |
| F7.5 | Sensitive bank-change verification | M | NOT_STARTED | — |
| F7.6 | Profile/change pages + TPA queue | M | NOT_STARTED | — |
| F7.7 | Network improvement plan | S | NOT_STARTED | — |

## F8 — Provider performance scorecards

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F8.1 | Approve versioned metric catalog | M | GATED(multi-owner approval §8.13) | — |
| F8.2 | Extend scorecard schema + watermarks | M | NOT_STARTED | — |
| F8.3 | Deterministic scorecard refresh (per family) | M/fam | NOT_STARTED | — |
| F8.4 | Anonymized cohort benchmarks | M | NOT_STARTED | — |
| F8.5 | Provider performance dashboard | M | NOT_STARTED | — |
| F8.6 | TPA network performance workspace | M | NOT_STARTED | — |

## F9 — HMS integration control plane

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F9.1 | Inventory integration configs/secrets/paths | S | NOT_STARTED | — |
| F9.2 | Integration connection/delivery schema | M | NOT_STARTED | — |
| F9.3 | Connection + credential administration | M | NOT_STARTED | — |
| F9.4 | Durable inbound delivery receipt | M | NOT_STARTED | — |
| F9.5 | Route inbound HMS records canonically (per type) | M/type | NOT_STARTED | — |
| F9.6 | Retry, poison quarantine, sweeper | M | NOT_STARTED | — |
| F9.7 | One contracted outbound pull adapter | L | GATED(signed contract + sandbox) | — |
| F9.8 | Provider/admin integration ops views | M | NOT_STARTED | — |
| F9.9 | Cut over legacy HMS configuration/path | M | GATED(pilot sign-off) | — |

## F10 — Capitation/PMPM extension

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F10.1 | Approve arrangement/accounting spec | M | GATED(finance/legal/network/client/provider) | — |
| F10.2 | Capitation arrangement/period/adjustment schema | M | NOT_STARTED | — |
| F10.3 | Eligible-life snapshot | M | NOT_STARTED | — |
| F10.4 | Calculate/freeze capitation accrual | M | NOT_STARTED | — |
| F10.5 | Link encounters + protect carve-outs | M | NOT_STARTED | — |
| F10.6 | Capitation statement, approval, payment | L | NOT_STARTED | — |
| F10.7 | Capitation pilot: three reconciled periods | gate | GATED(pilot sign-off) | — |

## F11 — System hardening, UAT, rollout

| Pkg | Title | Size | Status | Evidence |
|---|---|---|---|---|
| F11.1 | Cross-provider/branch/role security suite | M | NOT_STARTED | — |
| F11.2 | Concurrency/idempotency suite | M | NOT_STARTED | — |
| F11.3 | Money conservation suite | M | NOT_STARTED | — |
| F11.4 | Privacy/log/export scan | M | NOT_STARTED | — |
| F11.5 | Performance/load tests | M | NOT_STARTED | — |
| F11.6 | Accessibility + responsive UAT | M | NOT_STARTED | — |
| F11.7 | Operations + incident runbooks | M | NOT_STARTED | — |
| F11.8 | Actor-based end-to-end UAT | gate | GATED(human actors + sign-off) | — |
| F11.9 | Pilot-provider activation | gate | GATED(sign-offs + runbooks) | — |
| F11.10 | GA + legacy retirement (multiple pkgs) | multi | GATED(pilot + product sign-off) | — |
